using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GroundMovement : MonoBehaviour
{
    //private float initialY, initialZ;
    //private float initialPlayerY, initialPlayerZ;
    //[SerializeField] private Transform player;

    //// Start is called before the first frame update
    //void Start()
    //{
    //    initialY = transform.position.y;
    //    initialZ = transform.position.z;

    //    initialPlayerY = player.position.y;
    //    initialPlayerZ = player.position.z;
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    transform.position = new Vector3(transform.position.x, (player.position.y + initialPlayerY), (player.position.z + initialPlayerZ));
    //}
}
